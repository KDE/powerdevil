
# Discover what to do here, as probably we need this dbus file. but this is
# not related to the config anymore.

# qt5_add_dbus_interface(actionconfig_SRCS
#    ${CMAKE_SOURCE_DIR}/daemon/backends/upower/dbus/org.freedesktop.UPower.xml
#    upower_interface)
